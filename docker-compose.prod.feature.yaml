name: koru-feature

services:
  dummy-service:
    container_name: Dummy-Service
    image: koru-feature-dummy-service:latest
    environment:
      - ENV_PATH=/service/.env
    networks:
      - koru-network

networks:
  koru-network:
    external: true